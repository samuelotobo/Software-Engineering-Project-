The Library Kiosk return process begins when the student places a book on the scanner. The system scans and validates the book, then checks whether it is overdue. If the book is overdue, the system calculates a fine and only continues if the student pays it. After the return is accepted, the system updates inventory, sends a notification, and places the book back on the shelf in parallel.